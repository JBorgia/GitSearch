import { HideApiUrlPipe } from './hide-api-url.pipe';

describe('HideApiUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new HideApiUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
